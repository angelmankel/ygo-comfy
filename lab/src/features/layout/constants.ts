// TopNav geometry: 8px wrapper padding (top) + 48px bar (h-12) + 8px wrapper
// padding (bottom) = 64px. The canvas/content area sits below this so the
// image never butts against the bar.
export const NAV_HEIGHT = 64;
export const SIDENAV_W = 52;
export const LEFT_W = 380;
export const RIGHT_W = 340;
export const TOOLBAR_TRANSITION = 'left 200ms ease-out, right 200ms ease-out';
