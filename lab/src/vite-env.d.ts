/// <reference types="vite/client" />
// Brings in `import.meta.env` (DEV, PROD, BASE_URL …). Vite always defines these; without this
// reference TypeScript does not know the `env` property exists on ImportMeta.
